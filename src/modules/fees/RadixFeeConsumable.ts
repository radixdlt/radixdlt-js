

import {RadixConsumable, 
    RadixBase64,
    RadixEncryptor,
    RadixKeyPair,
    } from '../atom_model'

export default class RadixFeeConsumable extends RadixConsumable {
  public static SERIALIZER = 2136527433
}
